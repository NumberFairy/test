package com.wisdombud.alumni.dao.manage.impl;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Repository;

import com.wisdombud.alumni.dao.manage.BaseTimelineDao;
import com.wisdombud.alumni.pojo.manage.BaseTimeline;

import common.toolkit.java.orm.hibernate.SimpleHibernateDao;

/**
 * 校友成长时光轴 DaoImpl. <br/>
 * 
 * @author Administrator
 * 
 */
@Repository(value = "baseTimelineDao")
public class BaseTimelineDaoImpl extends SimpleHibernateDao<BaseTimeline, String> implements BaseTimelineDao {
	private static final Logger LOGGER = Logger.getLogger(BaseTimelineDaoImpl.class);
}
