package com.wisdombud.alumni.dao.manage.impl;

import org.springframework.stereotype.Repository;
import org.apache.log4j.Logger;
import com.wisdombud.alumni.pojo.manage.HomeNewsPic;
import com.wisdombud.alumni.dao.manage.HomeNewsPicDao;

import common.toolkit.java.orm.hibernate.SimpleHibernateDao;
	/**
	 * 微信轮播图片管理 DaoImpl. <br/>
     * @author Administrator
     
     */
@Repository(value="homeNewsPicDao")
	public class HomeNewsPicDaoImpl extends SimpleHibernateDao<HomeNewsPic, String> implements HomeNewsPicDao {
	private static final Logger LOGGER = Logger.getLogger(HomeNewsPicDaoImpl.class);
}
