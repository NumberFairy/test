package com.wisdombud.alumni.dao.manage.impl;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Repository;

import com.wisdombud.alumni.dao.manage.DicChapterBranchDao;
import com.wisdombud.alumni.pojo.dic.DicChapterBranch;
import common.toolkit.java.orm.hibernate.SimpleHibernateDao;

/**
 * 地方分会 DaoImpl. <br/>
 *
 * @author Administrator
 *
 */
@Repository(value = "dicChapterBranchDao")
public class DicChapterBranchDaoImpl extends SimpleHibernateDao<DicChapterBranch, String> implements DicChapterBranchDao {
	private static final Logger LOGGER = Logger.getLogger(DicChapterBranchDaoImpl.class);
}
