package com.wisdombud.alumni.dao.manage.impl;

import org.springframework.stereotype.Repository;
import org.apache.log4j.Logger;
import com.wisdombud.alumni.pojo.manage.${entityName};
import com.wisdombud.alumni.dao.manage.${entityName}Dao;

import common.toolkit.java.orm.hibernate.SimpleHibernateDao;
	/**
	 * ${comment} DaoImpl. <br/>
     * @author Administrator
     
     */
@Repository(value="${lowerEntityName}Dao")
	public class ${entityName}DaoImpl extends SimpleHibernateDao<${entityName}, String> implements ${entityName}Dao {
	private static final Logger LOGGER = Logger.getLogger(${entityName}DaoImpl.class);
}
